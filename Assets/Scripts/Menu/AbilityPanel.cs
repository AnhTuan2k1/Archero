using UnityEngine;
using UnityEngine.UI;

public class AbilityPanel : MonoBehaviour
{
    //[SerializeField] GameObject abilityTemplate;
    //[SerializeField] Transform parentTransform;

    //public void SpawnAbility()
    //{
    //    GameObject g;
    //    for (int i = 0; i < 3; i++)
    //    {
    //        g = Instantiate(abilityTemplate, parentTransform);
    //        AbilityConfig ability = AbilityManager.Instance.getAnRandomAbilityConfig();

    //        g.transform.GetChild(1).GetComponent<Image>().sprite = ability.image;
    //        g.transform.GetChild(2).GetComponent<Text>().text = ability.title;
    //        g.transform.GetChild(3).GetComponent<Text>().text = ability.description;
    //    }
    //}
}